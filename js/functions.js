//#region appModel
/**
 * @param {AppModel} model
 * @param {string} name
 * @returns {AppModel}
 */
function setTitle(model, name) { }
//slides
/**
 * @param {AppModel} model
 * @returns {AppModel}
 */
function addSlide(model) { }
/**
 * @param {AppModel} model
 * @returns {AppModel}
 */
function deleteSlide(model) { }
/**
 * @param {AppModel} model
 * @param {number} targetNumber
 * @returns {AppModel}
 */
function moveSlide(model, targetNumber) { }
/**
 * @param {AppModel} model
 * @param {string} color
 * @returns {AppModel}
 */
//#endregion
//#region slide
function setBackColor(model, color) { }
/**
 * @param {AppModel} model
 * @param {string} imgData
 * @returns {AppModel}
 */
function setBackImg(model, imgData) { }
/**
 * @param {AppModel} model
 * @returns {AppModel}
 */
function deleteElement(model) { }
/**
 * @param {AppModel} model
 * @param {Point} leftTop
 * @param {Point} rightBottom
 * @returns {AppModel}
 */
function addText(model, leftTop, rightBottom) { }
/**
 * @param {AppModel} model
 * @param {string} imgData
 * @param {Point} leftTop
 * @returns {AppModel}
 */
function addImg(model, imgData, leftTop) { }
/**
 * @param {AppModel} model
 * @param {Point} position
 * @param {number} radius
 * @returns {AppModel}
 */
function addCircle(slide, position, radius) { }
/**
 * @param {AppModel} model
 * @param {Point} leftTop
 * @param {Point} rightBottom
 * @returns {AppModel}
 */
function addRectangle(model, leftTop, rightBottom) { }
/**
 * @param {AppModel} model
 * @param {Point} pos1
 * @param {Point} pos2
 * @param {Point} pos3
 * @returns {AppModel}
 */
function addTriangle(model, pos1, pos2, pos3) { }
/**
 * @param {AppModel} model
 * @param {nuber} layer
 * @returns {AppModel}
 */
//#endregion 
function setLayer(model, layer) { }
/**
 * @param {AppModel} model
 * @param {string} str
 * @returns {AppModel}
 */
function setStr(model, str) { }
/**
 * @param {AppModel} model
 * @param {number} fontNum
 * @returns {AppModel}
 */
function setFont(model, fontNum) { }
/**
 * @param {AppModel} model
 * @param {number} fontSize
 * @returns {AppModel}
 */
function setFontSize(model, fontSize) { }
/**
 * @param {AppModel} model
 * @param {Point} leftTop
 * @returns {AppModel}
 */
function moveText(model, leftTop) { }
/**
 * @param {AppModel} model
 * @param {Point} leftTop
 * @returns {AppModel}
 */
function setLeftTop(model, leftTop) { }
/**
 * @param {AppModel} model
 * @param {Point} rightBottom
 * @returns {AppModel}
 */
function setRightBottom(model, rightBottom) { }
/**
 * @param {AppModel} model
 * @param {Point} pos
 * @returns {AppModel}
 */
//img
function moveImg(model, pos) { }
/**
 * @param {AppModel} model
 * @param {number} width
 * @returns {AppModel}
 */
function setWidth(model, width) { }
/**
 * @param {AppModel} model
 * @param {number} height
 * @returns {AppModel}
 */
function setHeight(model, height) { }
/**
 * @param {AppModel} model
 * @param {Point} pos
 * @returns {AppModel}
 */
function moveCircle(model, pos) { }
/**
 * @param {AppModel} model
 * @param {number} radius
 * @returns {AppModel}
 */
function setRadius(model, radius) { }
/**
 * @param {AppModel} model
 * @param {string} color
 * @returns {AppModel}
 */
function setColor(model, color) { }
/**
 * @param {AppModel} model
 * @param {string} borderColor
 * @returns {AppModel}
 */
function setBorderColor(model, borderColor) { }
/**
 * @param {AppModel} model
 * @param {Point} pos
 * @returns {AppModel}
 */
function moveRect(model, pos) { }
/**
 * @param {AppModel} model
 * @param {Point} pos
 * @returns {AppModel}
 */
function moveTrian(model, pos) { }
/**
 * @param {AppModel} model
 * @param {Point} pos
 * @returns {AppModel}
 */
function setPos1(model, pos) { }
/**
 * @param {AppModel} model
 * @param {Point} pos
 * @returns {AppModel}
 */
function setPos2(model, pos) { }
/**
 * @param {AppModel} model
 * @param {Point} pos
 * @returns {AppModel}
 */
function setPos3(model, pos) { }